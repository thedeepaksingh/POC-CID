{
  "name": "poc-cid",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client-app && npm install --prefix server-app",
    "build": "npm run build --prefix client-app && npm run build --prefix server-app",
    "start": "npm run start --prefix server-app"
  },
  "dependencies": {
    "next": "^15.5.6",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
